#include <stdio.h>
#include <string.h>
#include <stdlib.h>


char * 
bigadd(char *a, char*b)
{
    int alen = strlen(a);
    int blen = strlen(b);
    int max_len = (alen>blen)?(alen+1):(blen+1);

}

int
main(int argc, char *argv[])
{
    


    return 0;
}

